# Workspace

This is the fast writable workspace for the Skydnir test suite. It is used for
temporary test traffic and should stay on app-private storage unless a specific
test intentionally targets `/documents` or `/shared`.
